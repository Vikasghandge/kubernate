apiVersion: v1
kind: apiVersion: v1
kind: Pod
metadata:
  name: apache-pod
  labels:
    name: apache-pod
sp
  containers:
  - name: apache-
    image: <Image>
    resources:
      limi
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
