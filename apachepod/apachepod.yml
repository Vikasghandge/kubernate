apiVersion: v1
kind: apiVersion: v1
kind: Pod
metadata:
  name: 
  labels:
    name: 
spec:
  containers:
  - name: 
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
