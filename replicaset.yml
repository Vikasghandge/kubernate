apiVersion: apps/v1
kind: ReplicaSet
metadata: 
   name:  replica-in
   labels: 
      app: replica-in
spec:
    replicas: 4
    template:  
     metadata:
       name:  replica-in
       labels:
         app: replica-in
    spec:
      containers:
       - name: nginxcon
         image: nginx:latest
         resources: 
            request: 
              cpu: "200m"
              memory: "128Mi"         
         ports:
          - protocol: TCP
            containerPort: 80
            name:  http 
    selector:
        matchExpressions:
          - {key: app, operator: In, values}     