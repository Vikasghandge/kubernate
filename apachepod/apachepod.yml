apiVersion: v1
kind: pod 
metadata: 
  name: apache-pod
  labels:
    apps: apache-pod
spec: 
  containers: 
      - name: apache-container
        image:  httpd:latest
        ports: 
        - containerPort: 80
